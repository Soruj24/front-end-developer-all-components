/** A community comment left on a component page. */
export interface RegistryComment {
  id: string;
  author: string;
  avatarColor: string;
  content: string;
  createdAt: string;
  likes: number;
}
