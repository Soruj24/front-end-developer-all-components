import { modelCatalog, PROVIDER_BASE_URLS } from "../../constants";

/** Server-side access to the static model catalog (client-safe data). */
export { modelCatalog as builtinModels, PROVIDER_BASE_URLS };
