import { buttonVariants } from "./button-variants";
import { buttonSizes } from "./button-sizes";
import { buttonDisabled } from "./button-disabled";
import { buttonLoading } from "./button-loading";
import { buttonWithIcons } from "./button-with-icons";
import { buttonFullWidth } from "./button-full-width";
import { buttonGroup } from "./button-group";
import { buttonIconOnly } from "./button-icon-only";
import type { RegistryEntry } from "../../types";

/** Individual examples. Each is one preview block on the Buttons page. */
export const buttons: RegistryEntry[] = [
  buttonVariants,
  buttonSizes,
  buttonDisabled,
  buttonLoading,
  buttonWithIcons,
  buttonFullWidth,
  buttonGroup,
  buttonIconOnly,
];
