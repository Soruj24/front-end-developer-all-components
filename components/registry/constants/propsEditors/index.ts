import { propsEditorFull } from "./props-editor-full";

import type { RegistryEntry } from "../../types";

/** Visual Props Editor examples. */
export const propsEditors: RegistryEntry[] = [propsEditorFull];
